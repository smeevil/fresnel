#!/usr/bin/env ruby
require 'fresnel'
require 'fresnel/cli'

COLOR=false
CACHE=true
CACHE_TIMEOUT=500.seconds
TERM_SIZE=80
DEBUG=false

Signal.trap("SIGINT") {
  puts "Stopping..."
  exit(0)
}

Cli.new(ARGV).run!